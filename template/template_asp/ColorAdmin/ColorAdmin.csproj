<Project Sdk="Microsoft.NET.Sdk.Web">
  <!--
  <Target Name="MyPreCompileTarget" BeforeTargets="Build">
    <Exec Command="gulp" />
  </Target>
  -->
  <PropertyGroup>
    <TargetFramework>netcoreapp3.1</TargetFramework>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'AspStudio' " />
</Project>
